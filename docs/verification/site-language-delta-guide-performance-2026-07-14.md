# 网站语言、Delta 引导与性能验证报告

日期：2026-07-14  
状态：通过，摄像头硬件实测待用户授权

## 验证范围

- 网站默认中文与语言选择保留。
- Delta 独立返回按钮、首次使用引导及桌面/移动端布局。
- 网站装饰动画预算与美颜相机帧调度。
- 构建产物同步、站点结构和浏览器错误。

## 自动化结果

- `pp-tools/frontend`：29 个测试文件、74 项测试全部通过。
- `pp-tools/frontend`：生产构建与嵌入构建通过。
- `My`：24 项测试全部通过。
- `My`：站点检查通过，19 个必需路径无缺失。
- 嵌入 JS、CSS 与最新构建产物的 SHA-256 完全一致。

## 浏览器结果

- 首页默认显示中文；切换语言并刷新后仍保留选择，结束前已恢复中文。
- Delta 返回按钮可回到工具中心，四步引导会按连接状态标记当前步骤。
- 桌面布局无顶部空带或组件重叠。
- 390 x 844 视口无横向溢出；返回、连接、引导和上传区域无重叠。
- 首页前台采样 120 帧：P50 为 3.1 ms，P95 为 3.2 ms，最大 46.8 ms，仅 1 帧超过 25 ms。该结果用于确认没有固定低帧率上限，不代表显示器物理刷新率。
- 系统启用“减少动态效果”时，装饰动画会按预期暂停。
- 已检查 Delta、相机和首页测试页，浏览器控制台无错误。

## 同步说明

标准同步命令在替换正在占用的 `Delta-Companion.exe` 时被 Windows 文件锁阻止。由于本轮未修改 Companion，验证时保留现有 EXE，并非破坏式同步其余 15 个网页与资源文件；最终目标目录共 16 个文件，嵌入构建哈希一致。

## 未覆盖项

未在浏览器中点击“开启相机”，因为该操作需要用户主动授予摄像头权限。相机最高可用帧率约束、能力不足回退、视频帧调度和识别自适应间隔已由自动化测试覆盖；真实摄像头启动、切换、手势和拍照仍需授权后人工复核。
